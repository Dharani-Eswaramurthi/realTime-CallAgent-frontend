Quick start

1. Install deps

   npm install

2. Configure backend URL (optional)

   Create a `.env` file with:

   VITE_BACKEND_URL=http://localhost:8000

3. Run dev server on port 3000

   npm run dev


